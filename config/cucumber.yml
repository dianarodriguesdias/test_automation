qat: &qat >
  --format QAT::Formatter::Console
  --format html --out public/index.html
  --format junit --out public/
  --format json --out public/cucumber.json
  --strict

standard: &standard >
  --format html --out public/index.html
  --format junit --out features/reports

flat: &flat --format pretty

default: *qat